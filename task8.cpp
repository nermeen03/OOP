#include <iostream>

using namespace std;
/*
int main()
{
    int Max,Min;
    int arr[5];
    for(int i=0;i<5;i++){
        cout<<"enter a number: ";
        cin>>arr[i];
    }
    Max = Min = arr[0];
    for(int i=1; i<5;i++){
        if(Max<arr[i])
            Max=arr[i];
        if(Min>arr[i])
            Min=arr[i];
    }
    cout<<"The max is: "<<Max<<endl;
    cout<<"The min is: "<<Min<<endl;
    return 0;
}

*/
